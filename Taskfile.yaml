# Docs: https://taskfile.dev/#/usage
version: "3"

tasks:
  install:
    cmds:
      - uv sync

  format:
    cmds:
      - uv run ruff format .

  lint:
    cmds:
      - uv run ruff check --fix .
      - uv run ty check .

  test:
    cmds:
      - uv run pytest .

  build:
    cmds:
      - uv build

  run:
    cmds:
      - uv run ***tracker {{.CLI_ARGS}}

  run:fetch:
    cmds:
      - uv run ***tracker fetch

  run:server:
    cmds:
      - uv run ***tracker serve {{.CLI_ARGS}}

  docker:build:
    cmds:
      - docker build . --tag ***tracker

  docker:run:
    cmds:
      - docker compose up --build
